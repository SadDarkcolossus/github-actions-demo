name: GitHub Actions Demo

on: push

jobs:
  explore-github-actions:
    runs-on: ubuntu-latest

    steps:
      - name: Saludo inicial
        run: echo "🎉 ¡Hola mundo desde GitHub Actions!"

      - name: Mensaje con commit
        run: echo "Este trabajo se ejecutó en el commit de: ${{ github.event.head_commit.message }}"
